@model Homepage.ViewModels.Login

@{ 
    ViewBag.Title = "Home";
}

    <div id="main">
        <h1>Please, login!</h1>
        <form method="post">
            <div>
                <label asp-for="Email"></label>
                <input asp-for="Email"/>
                <span asp-validation-for="Email"></span>
            </div>
            <div>
                <label asp-for="Password"></label>
                <input asp-for="Password" />
                <span asp-validation-for="Password"></span>
            </div>
            <div id="submit">
                <input type="submit" />
            </div>
        </form>
    </div>
